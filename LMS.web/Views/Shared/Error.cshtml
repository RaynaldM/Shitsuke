@using aspnet_mvc_helpers
@using LMS.Resources
@model HandleErrorInfo

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <title>@Global.ErrorPage</title>
    @Html.Css("~/content/app.css")
</head>
<body>
    <div class="container body-content">
        <div class="jumbotron">
            <h1>
                @Global.Error
            </h1>
            <p>
                @Global.ErrorOccured
            </p>
            <p>
                @if (Request.IsLocal)
                {
                    if (@Model != null && @Model.Exception != null)
                    {
                        <h4>@Model.Exception.Message</h4>
                        <h6>Source : @Model.ControllerName / @Model.ActionName</h6>
                        <h5>StackTrace</h5>
                        <code>@Model.Exception.StackTrace</code>
                    }
                    else
                    {
                        <h3>@Global.ErrorNullException</h3>
                    }
                }
                else
                {
                    <h4>@Global.ErrorReportedToAdmin</h4>
                }
            </p>
            <p>
                <a class="btn btn-primary btn-lg" href="@Url.Action("Index", "Home")" role="button">@Global.ErrorBackHome</a>
            </p>
        </div>
    </div>
</body>
</html>
